const msgpack = require('@msgpack/msgpack');

class Encoder {

    constructor(options = {}) {
        this.soptions = options;
    }

    encode () {
        return turn [msgpack.encode(packet, options)];
    }
}

exports = Encoder;
